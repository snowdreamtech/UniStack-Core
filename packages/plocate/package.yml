---
apiVersion: "v1alpha1"
kind: "package"

metadata:
  name: "plocate"
  version: "0.0.0"
  description: "Auto-generated UniStack core package for plocate"
  authors: ["snowdreamtech <snowdreamtech@qq.com>"]
  homepage: "https://repology.org/project/plocate"
  license: "Unknown"
  tags: ["core", "auto-generated", "service"]

compatibility:
  - os: "linux"
    arch: ["amd64", "arm64", "386", "arm"]

delivery:
  type: "app"
  
dependencies:
  required: {}
  recommended: {}

spec:
  packages:
    debian: "plocate"
    rhel: "plocate"
    alpine: "plocate"
  services:
    debian: "plocate-updatedb"
    rhel: ""
    alpine: ""
